/**
 * Offtake partners for the Plastic Credits UI are defined in `impact.ts`
 * with bundled logos from `src/assets/offtake-partners/`.
 *
 * For partner logos loaded by public URL only (no import), use
 * `public/branding/offtake-partners/logos/` and paths like
 * `/branding/offtake-partners/logos/foo.png`.
 */
export { offtakePartners } from './impact'
